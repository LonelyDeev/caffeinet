# 📋 سند معماری و برنامه‌ریزی — استارتاپ «کافی‌نت آنلاین»

> نسخه ۱.۱۳ — نقشهٔ راه کامل شد: فازهای ۰ تا ۱۱ تکمیل شده‌اند (آخرین: **امنیت و لانچ** — رمزنگاری مدارک AES-256-GCM + پاکسازی دوره‌ای + hardening + مستندات API + دپلوی). آمادهٔ انتشار پروداکشن.

---

## ۱) نمای کلی محصول

پلتفرمی که **مشتریان** خدمات کافی‌نت (خودرو، قضایی، مالیاتی، دانشگاه و…) را آنلاین سفارش می‌دهند؛
درخواست در **۶۰ ثانیه** بین **کافی‌نت‌ها** پخش می‌شود، اولین کافی‌نتِ قبول‌کننده آن را **قفل** می‌کند،
**اپراتور** از طریق **چت تلگرام‌گونه** با مشتری تعامل می‌کند و پس از **پرداخت**، خدمت انجام و
**کمیسیونها** به‌صورت خودکار بین **پلتفرم / سازمان / کافی‌نت / اپراتور** تسویه می‌شود.

### نقش‌ها و پنل‌ها

| نقش | پنل | مسیر | کنترلر | ویو |
|---|---|---|---|---|
| مدیر کل (مالک پلتفرم) | Super Admin | `/admin` | `app/Http/Controllers/Back/Admin` | `resources/views/back/admin` |
| سازمان (حقوقی/حقیقی) | Organization | `/organization` | `app/Http/Controllers/Back/Org` | `resources/views/back/org` |
| مدیر کافی‌نت | Coffee-net Manager | `/coffeenet` | `app/Http/Controllers/Back/Coffeenet` | `resources/views/back/coffeenet` |
| اپراتور / کارمند | Operator | `/operator` | `app/Http/Controllers/Back/Operator` | `resources/views/back/operator` |
| مشتری | اپ موبایل (فقط API) | `/api/v1/*` | `app/Http/Controllers/Api/V1` | — |

- **کافی‌نت** می‌تواند مستقل باشد یا زیرمجموعه یک سازمان (فیلد `organization_id` اختیاری).
- **کارمند** همیشه زیرمجموعه یک کافی‌نت است.
- هر نقش می‌تواند **دسترسی‌های زیرین خود** را شخصی‌سازی کند (Spatie Permission + اسکوپ).

---

## ۲) تصمیم‌های فنی

| موضوع | تصمیم | دلیل |
|---|---|---|
| فریمورک | Laravel 13 + PHP 8.5 | درخواست مالک؛ جدیدترین نسخه پایدار |
| احراز هویت پنل‌ها | Session (Laravel Auth) | امن و ساده برای وب‌پنل |
| احراز هویت مشتری | Sanctum (Token) | اپ موبایل/کلاینت |
| نقش و دسترسی | `spatie/laravel-permission` + **Role Scoping کافی‌نت** | پکیج استاندارد + اسکوپ چند-اجاره |
| فرانت پنل‌ها | **Blade + jQuery + Tailwind 4 (CSS کامپایل استاتیک)** — صفر وابستگی Node (بدون Vite/npm/package.json)؛ JS هر صفحه در فایل جدا: پنل‌ها `public/back/assets/js/pages/{ماژول}/{صفحه}.js` — اپ مشتری `public/front/assets/js/pages/{صفحه}.js` — مشترک‌ها `public/assets/` | درخواست مالک: JS فقط Blade+jQuery، حتماً فایل جدا و لینک‌شده؛ CSS کامپایل‌شده به‌صورت استاتیک (`public/assets/css/app.css`) سرو می‌شود |
| دیتابیس | SQLite (dev) — MySQL 8 (پروداکشن) | قابل حمل با Eloquent |
| صف و زمان‌بندی | `database` queue + Scheduler (cron در پروداکشن) | بدون Redis در شروع |
| چت | Polling AJAX (۲–۳ ثانیه) — ارتقا به WebSocket در فاز آتی | شروع سبک؛ معماری آماده ارتقا |
| پیامک | Abstraction با درایور `log` (dev) + درایور **کاوه‌نگار** (پروداکشن) | تست‌پذیر بدون اعتبار |
| پرداخت | Abstraction با درایور `mock` (dev) + درایور **زرین‌پال** | تست‌پذیر بدون درگاه واقعی |
| فایل مدارک | دیسک `private` + URLهای موقت امضاشده | امنیت مدارک (ریسک شماره ۳ CN3) |

---

## ۳) مدل داده (خلاصه)

### هسته کاربران و ساختار
- `users` — همه انسان‌های سیستم؛ فیلدهای پروفایل مشتری (نام، فامیل، جنسیت، استان، شهر، تاریخ تولد، `profile_completed`)
- `roles`, `permissions`, `model_has_roles`, `model_has_permissions`, `role_has_permissions` — Spatie
- `staff_assignments` — عضویت کارمند در کافی‌نت + نقش درون-کافی‌نتی + تنظیمات حقوق
- `organizations` — سازمان (نام، کد/شناسه، وضعیت، کیف پول)
- `coffeenets` — کافی‌نت (`organization_id` **nullable** = مستقل)، وضعیت تأیید مدیریت کل

### خدمات و فرم‌ساز
- `service_categories` — درخت دسته‌بندی (parent_id)
- `services` — خدمت + قیمت مشتری + زمان تقریبی + وضعیت
- `service_costs` — ردیف‌های هزینه/کارمزد خدمت با **`is_commission`** (فلگِ «جزو کمیسیون باشد؟») و `type` (expense | fee)
- `service_form_fields` — فیلدهای داینامیک (نوع: text/number/mobile/national_code/select/date/file/textarea، required، ruleهای اعتبارسنجی، ترتیب، options JSON)
- `service_versions` — snapshot نسخه فرم/هزینه هر خدمت (سفارش به نسخهٔ ثبت‌شده اشاره می‌کند)

### سفارش و چت
- `orders` — سفارش (`customer_id`, `service_id`, `service_version_id`, `coffeenet_id`, `operator_id`, `form_data JSON`, وضعیت، مبالغ snapshot)
- `order_status_history` — تاریخچه وضعیت + کاربر تغییردهنده
- `order_files` — مدارک مشتری/خروجی اپراتور (نوع: document | result)
- `conversations` — گفتگو سفارش (customer ↔ operator)
- `messages` — پیام (نوع: text/image/audio/video/file، seen_at)

### مالی
- `wallets` — کیف پول (polymorphic: user/org/coffeenet) + `transactions` (دوطرفه دفتری، تغییرناپذیر)
- `payments` — پرداخت آنلاین (درایور، ref_id، وضعیت)
- `commission_settings` — قواعد کمیسیون: سهم **پلتفرم/سازمان/کافی‌نت/اپراتور** (درصد یا مبلغ ثابت؛ سراسری یا هر خدمت)
- `salary_settings` — مدل حقوق کافی‌نت برای کارمند (percent | fixed_per_order | monthly) + نرخ
- `salary_logs` — لاگ واریز ماهیانه/اضافه‌کار (فقط ثبت گزارشی)
- `commission_payouts` — تسویه هر سفارش موفق (فقط مبالغ با `is_commission=true` وارد محاسبه می‌شوند)
- `referral_settings` / `referral_rewards` — پاداش معرفی کافی‌نت توسط سازمان (مبلغ اولیه + درصد/مبلغ از کار)
- `withdrawals` — درخواست برداشت سازمان (و کافی‌نت در آینده)

### پشتیبانی
- `tickets` + `ticket_messages` — تیکت/شکایت مشتری
- `otp_codes` — OTP هش‌شده با انقضا
- `notifications` + `sms_logs` + `audit_logs` — اعلان، پیامک، لاگ فعالیت

### جغرافیا
- `provinces`, `cities` — داده کامل ایران (seed)

### ماشین وضعیت سفارش (Order State Machine)

```
pending_payment ──► paid ──► broadcasting(60s) ──► accepted ──► in_progress ──► delivered ──► completed
     │                │              │ (بدون قبول)                    │
     │                │              ▼                                ▼
  cancelled         cancelled   queued_sms (اطلاع با پیامک؛     needs_info ──► in_progress
  (با دلیل)        (با دلیل)     تعیین‌تکلیف توسط ادمین)          refunded / cancelled
```

> قواعد: تغییر وضعیت فقط از مجازهای تعریفشده؛ هر تغییر در `order_status_history` + اعلان + لاگ ثبت می‌شود.

---

## ۴) جریان‌های کلیدی

### جریان مشتری (API v1)
۱. `POST /otp/request` → ۲. `POST /otp/verify` (ساخت/یافتن کاربر + توکن Sanctum) → ۳. `POST /profile/complete` (اجباری: نام، نام‌خانوادگی، جنسیت، استان، شهر، تاریخ تولد) → ۴. `GET /categories/tree` و `GET /services` (جستجو/فیلتر) → ۵. `GET /services/{id}` (فرم داینامیک + هزینه‌ها) → ۶. `POST /orders` (form_data + مدارک) → ۷. پرداخت (`wallet` یا `online`) → ۸. `broadcasting` → صفحه «در انتظار پذیرش با تایمر ۶۰ ثانیه» → ۹. چت `GET/POST /orders/{id}/messages` (polling) → ۱۰. دریافت نتیجه و رسید.

### جریان کمیسیون (فقط مبالغ دارای فلگ is_commission)
سفارش موفق → `commission_payouts` با snapshot قواعد → افزایش تراکنساکشن کیف پول: سهم پلتفرم + سهم سازمان (اگر کافی‌نت زیرمجموعه) + سهم کافی‌نت + درآمد اپراتور (طبق `salary_settings`: درصد/مبلغ ثابت — ماهیانه فقط لاگ) — همه در یک Transaction دیتابیس.

---

## ۵) نقشه راه — ۱۲ فاز

| فاز | عنوان | محدوده (Deliverables) |
|---|---|---|
| **۰** ✅ | زیرساخت | محیط اجرا، Laravel 13 + Sanctum + Spatie، ساختار پوشه‌ها، RTL Theme، لندینگ |
| **۱** ✅ | پایه داده و دسترسی‌ها | تمام migrationها + modelها + seed استان/شهر + RBAC کامل + پنل ورود مدیر کل + مدیریت مدیران + تنظیمات + لاگ فعالیت |
| **۲** ✅ | سازمان‌ها و کافی‌نت‌ها | CRUD سازمان + کاربر مدیر، ثبت/تأیید کافی‌نت (مستقل/زیرمجموعه) + مدیر کافی‌نت، پنل سازمان (کیف پول، برداشت بلوکه، لیست/معرفی کافی‌نت)، پاداش معرفی خودکار، مدیریت برداشت‌ها توسط ادمین |
| **۳** ✅ | کارمندان و حقوق | پنل مدیر کافی‌نت، CRUD کارمند، salary_settings (درصدی/ثابت/ماهیانه)، لاگ حقوق، شخصی‌سازی دسترسی |
| **۴** ✅ | خدمات و فرم‌ساز | مدیریت دسته‌بندی + خدمت + `service_costs` با فلگ کمیسیون + **فرم‌ساز Drag&Drop** + پیش‌نمایش زنده + نسخه‌بندی خودکار snapshot |
| **۵** ✅ | API مشتری | OTP (هش + نرخ‌محدود + درایور لاگ)، پروفایل اجباری (شمسی)، کاتالوگ، سفارش با اعتبارسنجی فرم داینامیک ۱۱نوعی + مدارک، پرداخت کیف‌پول/آنلاین (shetabit: local/zarinpal)، تاریخچه + **اپ وب مشتری Blade+jQuery** (`/app`) — بدون Node |
| **۶** ✅ | موتور تخصیص سفارش | broadcasting با تایمر ۶۰s (قابل تنظیم) + انقضای تنبل روی هر poll + schedule:work، قفل اتمیک accept (lockForUpdate + ری‌چک وضعیت)، صف تعیین‌تکلیف + پیامک مشتری، ری‌پخش خودکار/دستی (سقف ۳ کوشش)، تخصیص دستی و لغو ادمین + پیامک، صندوق پخش زندهٔ پنل کافی‌نت (شمارش معکوس حلقه‌ای + polling ۴s)، مدیریت کامل سفارش‌های ادمین (چیپ آماری + فیلتر + مودال جزئیات کامل + تخصیص/ری‌پخش/لغو)، کارت پخش/صف/پذیرش در اپ مشتری با polling زنده |
| **۷** ✅ | چت تلگرام‌گونه | ChatService (گفتگو یکتا هر سفارش، پیام سیستمی گذارها، seen یک‌طرفه، seen_mine برای تیک دوتایی)، پیام text/image/audio/video/file با اعتبارسنجی حجم/فرمت + ذخیره در دیسک خصوصی + **لینک موقت امضاشده ۶ ساعته** (تصویر/صدا/ویدیو inline)، پولینگ افزایشی ۳s (فهرست ۶s + بج سایدبار 20s)، UI چت دو-سمتی (اپراتور: فهرست گفتگوها + صفحهٔ چت + عملیات سریع وضعیت با مجوز orders.update_status + پیامک تحویل؛ مشتری: کارت چت در جزئیات سفارش + API)، CSS مشترک chat.css دست‌نویس |
| **۸** ✅ | مالی و کمیسیون | **SettlementService** (موتور تسویهٔ idempotent روی Delivered/Completed + پرداخت‌شده: سهم پلتفرم + سازمانِ فقط-کافی‌نت‌های-زیرمجموعه + کافی‌نت + درآمد اپراتور از دل سهم کافی‌نت طبق salary_settings — همه در یک تراکنش DB + commission_payouts با snapshot + WalletService دفتری + AuditLogger)، قواعد کمیسیون (سراسری + اختصاصی هر خدمت با اولویت، اعتبارسنجی جمع ≤ ۱۰۰٪ — `/admin/commissions`)، تسویه‌ها با فیلتر نقش/تاریخ/سفارش + مودال جزئیات + retry (`/admin/settlements`)، گزارش مالی جامع (`/admin/finance`: حجم/مشمول/تفکیک سهم‌ها/روند ۶ ماه/همهٔ تراکنش‌ها)، کیف پول + برداشتِ کافی‌نت (`/coffeenet/{id}/wallet` + withdrawals با بلوکه و تعیین‌تکلیف ادمین)، درآمد و کیف پول اپراتور (`/operator/earnings`)، حساب سیستمی پلتفرم (platform@caffeinet.ir) |
| **۹** ✅ | داشبورد و گزارش | **AnalyticsService** مشترک ۴ پنل (scope: global/کافی‌نت/سازمان/اپراتور — سری روزانه/وضعیت/خدمات/اپراتور/مشتری/کافی‌نت + خلاصه + روند ویژهٔ اپراتور/کیف)؛ **PanelCharts** رَپر Chart.js 4.9 vendored (`vendor/chart.umd.min.js` — صفر وابستگی Node): line/bars/donut + ارقام فارسی + راست‌چین + همگام‌سازی خودکار با تم روشن/تاریک (رویداد ui:theme) + حالت خالی + رندر مجدد امن؛ داشبوردها: ادمین (روند ۱۴ روزه + دونات وضعیت)، کافی‌نت (سفارش + واریزی کیف + دونات)، سازمان (واریزی کیف ۳۰ روزه + سفارش کافی‌نت‌های زیرمجموعه)، اپراتور (تحویل + درآمد ۱۴ روزه — با گارد dashboard.access)؛ **گزارش تحلیلی جامع `/admin/analytics`**: فیلتر بازه (پریست ۷/۳۰/۹۰/ماه شمسی جاری/قبلی + سفارشی از/تا با سقف ۳۶۶ روز) → ۶ کارت خلاصه + ۵ نمودار/جدول (روند دو-محوره، دونات وضعیت، خدمات پرتقاضا، اپراتورها، کافی‌نت‌ها، مشتریان برتر)؛ **خروجی CSV** استریمی با BOM اکسل: ۵ اسکوپ گزارش تحلیلی (orders/services/operators/customers/coffeenets) + تراکنش‌های مالی با همان فیلترهای جدول (`/admin/finance/export`)؛ نمودارهای گزارش مالی (روند ۶ ماههٔ انباشته + دونات سهم نقش‌ها)؛ سایدبار ادمین: آیتم «گزارش تحلیلی» |
| **۱۰** ✅ | تیکت و اعلان‌ها | **TicketService** (چرخهٔ open→answered→customer_reply→closed + بازگشایی خودکار، شمارهٔ TK{ymd}-XXXX، پیوست تا ۱۵MB در دیسک خصوصی + **لینک موقت امضاشده ۶ ساعته** files/ticket، یادداشت داخلی فقط برای tickets.manage): **API مشتری** (/api/v1/tickets — ثبت/لیست/گفتگو/پاسخ/بستن + rate-limit) + **اپ مشتری** (/app/support — لیست+فیلتر چیپ + شیت ثبت با انتخاب سفارش + صفحهٔ گفتگو با حباب و پیوست inline)؛ **پنل ادمین** /admin/tickets (چیپ آماری + فیلتر وضعیت/اولویت/جستجو + ارجاع به کارشناس + تغییر اولویت + بستن/بازگشایی + پولینگ گفتگو)؛ **پنل کافی‌نت** (تیکت‌های سفارش‌های همان کافی‌نت با پاسخ/بستن) و **پنل اپراتور** (پاسخ عمومی — بدون یادداشت داخلی)؛ **NotificationService** (جدول استاندارد notifications): اعلان خودکار رخدادها — درخواست جدید (ادمین)، پذیرش/صف/لغو/تحویل (مشتری)، تسویهٔ کمیسیون (مدیر کافی‌نت+اپراتور+سازمان)، درخواست/تعیین‌تکلیف برداشت (ادمین↔درخواست‌دهنده)، تیکت (ادمین/مدیر کافی‌نت/مشتری/ارجاع) — همه fail-safe؛ **زنگ اعلان**: ۴ پنل (دکمه زنگ + دراپ‌داون با آیکون نوع + خواندم✓ + پولینگ بج ۳۰s) و اپ مشتری (شیت پایین + پولینگ ۲۵s) + API v1 (badge/data/read)؛ **درایور کاوه‌نگار** (KavenegarDriver + تنظیمات sms.kavenegar.*)؛ **قالب‌های پیامک**: جدول sms_templates + SmsTemplateService (متغیر {name} + کش ۶۰s اسکالر + fallback متن کد) + ۵ قالب seed شده + صفحهٔ /admin/sms-templates (ویرایش/فعال‌سازی/تست با پیش‌نمایش رندر + ارسال آزمایشی) |
| **۱۱** ✅ | امنیت و لانچ | **SecureFile (AES-256-GCM)**: همهٔ فایل‌های خصوصی (مدارک سفارش/پیوست گفتگو/پیوست تیکت) هنگام آپلود رمزنگاری و هنگام سرو با لینک امضاشده رمزگشایی می‌شوند + بک‌فیل files:encrypt (idempotent) + کلید FILE_ENCRYPTION_KEY مستقل (فالبک sha256(APP_KEY)) + **ثبت هر دانلود در لاگ فعالیت**؛ **Hardening**: 401 JSON استاندارد برای API مهمان (رفع 500 route[login]) + روت سراسری /login با ریدایرکت هوشمند پنل + میدل‌ور SecurityHeaders (nosniff/X-Frame/Referrer/Permissions + **CSP** با هش اسکریپت ضد-FOUC + گوگل‌فونت‌ مجاز) + سقف API ۱۲۰/دقیقه (throttle:api)؛ **پاکسازی دوره‌ای system:cleanup** (روزانه ۰۳:۳۰ + اجرای دستی): OTP منقضی + اعلان خوانده/نخوانده + لاگ پیامک/فعالیت طبق نگهداشت تنظیم‌شدنی + روتیشن حجمی لاگ (۱۰MB → ۵ بایگانی) + گزارش هر اجرا در settings؛ **صفحهٔ /admin/system**: چک‌لیست امنیت زنده (env/debug/https/کلیدها/نرخ‌ها) + متر پوشش رمزنگاری + برنامهٔ زمان‌بندی (زنده از schedule:list) + فرم نگهداشت + اجرای پاکسازی + مصرف منابع؛ **مستندات API /admin/api-docs**: ۲۷ اندپوینت در ۱۰ گروه (متد/مسیر/احراز/نرخ/ورودی/پاسخ نمونه/کدهای خطا) با جستجو/بازکردن/کپی/چاپ؛ **دپلوی**: .env.example کامل فارسی + DEPLOYMENT.md (نصب ۸ مرحله‌ای + چک‌لیست امنیت + Nginx + supervisor/cron + بکاپ/بازیابی + ارتقا + عیب‌یابی) |

> **قاعده:** هر فاز شامل تست مرورگر end-to-end + به‌روزرسانی همین سند و مرکز کنترل است.

---

## ۶) استانداردهای UI پنل‌ها

- تم گرم قهوه‌ای/کهربایی (Tailwind)، فونت **Vazirmatn**، RTL کامل
- اسکلت مشترک: `resources/views/back/layouts/panel.blade.php` (سایدبار + هدر + محتوا + فوتر چسبان)
- کامپوننت‌های Blade: card، stat، table، modal، toast، tabs (همگی با انیمیشن)
- **AJAX خالص** (fetch از `App.ajax`) برای ذخیره/بروزرسانی — بدون رفرش کامل صفحه
- دادههای جدول‌ها با صفحه‌بندی سروری + جستجو/فیلتر AJAX
- ریسپانسیو کامل (موبایل اول)

### ۶-۱) معماری فرانت «صفر وابستگی Node» (قید مالک)

```
public/
├── assets/                          # مشترک (لندینگ + همه پنل‌ها + اپ مشتری)
│   ├── css/app.css                  ← Tailwind کامپایل استاتیک (سورس: resources/css/app.css)
│   └── js/vendor/jquery.min.js      ← jQuery 3.7.1
├── back/assets/js/
│   ├── core.js                      ← window.App (ajax/toast/money/pageData)
│   └── pages/
│       ├── layout.js                ← مشترک ۳ لایهٔ پنل (سایدبار + خروج با data-logout-url)
│       ├── admin/…                  ← هر ویو: pages/{auth,admins,audit,coffeenets,…}/{صفحه}.js
│       ├── org/…                    ← pages/{auth,coffeenets,wallet,withdrawals}/…
│       └── coffeenet/…              ← pages/{auth,salaries,settings,staff}/…
└── front/assets/                    ← اپ مشتری (/app)
    ├── css/app.css                  ← CSS مستقل دست‌نویس
    └── js/{core.js + pages/…}       ← window.CN (نشست Sanctum + API v1)
```

- **قواعد:** هیچ JS درون‌خطی در Blade نوشته نمی‌شود؛ منطق هر صفحه در `pages/{ماژول}/{صفحه}.js` و با `asset()` لینک می‌شود
- **داده سرور → JS:** عنصر `‎<div id="page-data" hidden data-payload="{{ json_encode([...]) }}">‎` + `App.pageData()` (بدون کد درون‌خطی)
- **CSS:** خروجی کامپایل فعلی استاتیک سرو می‌شود؛ سورس طراحی در `resources/css/app.css` حفظ شده — برای کلاس Tailwind جدید، یک‌بار با **CLI مستقل Tailwind** (باینری رسمی بدون Node) کامپایل و جایگزین `public/assets/css/app.css` کنید
- **حذف شدند:** `package.json`، `vite.config.js`، `node_modules/`، `public/build/`، `resources/js/` و همهٔ `@vite`ها

---

## ۷) سوالات باز برای مالک (پاسخ پیش از فاز ۱)

۱. **پخش سفارش:** فقط بین کافی‌نت‌های **همان شهرستان** مشتری پخش شود؟ (پیشنهاد: بله، با قابلیت تنظیم شعاع/استان از پنل ادمین)
۲. **سقف ۶۰ ثانیه:** اگر هیچ کافی‌نتی قبول نکرد — سفارش در صف بماند تا تعیین تکلیف دستی ادمین، یا به‌صورت دوره‌ای دوباره پخش شود؟
۳. **پرداخت قبل یا بعد از پذیرش؟** طبق متن شما پرداخت پس از پذیرش است — تایید می‌کنم؟ (پیشنهاد: درگاه به‌محض ثبت سفارش قابل پرداخت باشد ولی الزام پرداخت قبل شروع کار اپراتور باشد)
۴. **درگاه پرداخت پروداکشن:** زرین‌پال؟ (درایور mock برای dev ساخته می‌شود)
۵. **درایور SMS:** کاوه‌نگار؟
۶. **دیتابیس پروداکشن:** MySQL؟
۷. **همه پنل‌ها فارسی/RTL** — تایید؟
