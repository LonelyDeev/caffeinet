# راهنمای دپلوی و راه‌اندازی پروداکشن — «کافی‌نت آنلاین»

> نسخهٔ سند: ۱.۲۷ (v37 — بازگشت پوش سیستمی به رفتار v34 «همیشه ارسال» + رفع نشانگر کرون + آفلاین‌شدن آنی)
> این سند برای سرور لینوکسی با PHP-FPM + Nginx نوشته شده است.

---

## ۱) پیش‌نیازها

| نیازمندی | نسخه | توضیح |
|---|---|---|
| PHP | ۸.۳+ (توصیه: ۸.۵) | افزونه‌های لازم پایین‌تر |
| Composer | ۲.x | نصب پکیج‌ها (بدون اسکریپت Node — پروژه صفر-Node است) |
| MySQL/MariaDB | ۸+ / ۱۰.۶+ | تصمیم مالک: پروداکشن MySQL |
| وب‌سرور | Nginx/Apache | با HTTPS (Let's Encrypt) |

**افزونه‌های PHP:** `openssl pdo pdo_mysql mbstring fileinfo gd curl iconv tokenizer xml ctype session` (+ `sqlite3` فقط برای محیط تست)

> پروژه از Tailwind CLI مستقل و Chart.js vendored استفاده می‌کند — **هیچ `npm install` / `npm run build` لازم نیست**؛ `public/assets` کامل و آمادهٔ سرو است.

---

## ۲) نصب

```bash
# ۱. دریافت سورس
git clone <repo> caffeinet && cd caffeinet

# ۲. نصب پکیج‌ها
composer install --no-dev --optimize-autoloader --no-interaction

# ۳. تنظیمات محیط
cp .env.example .env
php artisan key:generate

# ۴. کلید مستقل رمزنگاری فایل‌ها (توصیهٔ امنیتی فاز ۱۱)
php -r "file_put_contents('.env', PHP_EOL.'FILE_ENCRYPTION_KEY=base64:'.base64_encode(random_bytes(32)).PHP_EOL, FILE_APPEND);"

# ۵. پیکربندی دیتابیس (.env)
#    DB_CONNECTION=mysql و DB_HOST/DB_DATABASE/DB_USERNAME/DB_PASSWORD

# ۶. مایگریشن + دادهٔ اولیه (استان/شهر، تنظیمات، قالب پیامک، نقش‌ها، کاتالوگ ۸۱ خدمت)
php artisan migrate --force
php artisan db:seed --force

# ۷. کش پیکربندی
php artisan config:cache
php artisan route:cache
```

> **مهم:** اگر بعداً `.env` را تغییر دادید، `php artisan config:clear` سپس `config:cache` دوباره اجرا کنید.

> **تصاویر (فاز ۲۲):** سرو تصاویر خدمات/اطلاعیه‌ها از روت **`/media/{path}`** انجام
> می‌شود (MediaController) و **به symlink `public/storage` نیازی ندارد** — یعنی روی
> هر هاستی (حتی جایی که storage:link قابل اجرا نیست) کار می‌کند. seeder کاتالوگ
> تصاویر WebP را از `database/seeders/assets/services` داخل `storage/app/public/services`
> کپی می‌کند و اگر به‌جای symlink پوشهٔ واقعی قدیمی (خطای zip های قبلی) موجود باشد،
> خودکار پاک و symlink نسبی درست می‌سازد. اجرای `storage:link` اختیاری است.

---

## ۳) مجوزها

```bash
chown -R www-data:www-data storage bootstrap/cache
chmod -R ug+rwx storage bootstrap/cache
# دیتابیس SQLite فقط محیط dev است؛ در پروداکشن MySQL استفاده کنید.
```

فایل‌های خصوصی مشتری در `storage/app/private` با **AES-256-GCM** رمزنگاری می‌شوند — پشتیبان این پوشه + `.env` (کلید) با هم معنا دارند؛ بدون کلید، بکاپ فایل‌ها خوانا نیست.

---

## ۴) زمان‌بندی (باید همیشه فعال باشد)

سه رخداد زمان‌بندی‌شده (همه با همان یک خطِ کرون اجرا می‌شوند):

| کرون | کار |
|---|---|
| `* * * * *` | تعیین‌تکلیف سفارش‌های پخشِ منقضی (ری‌پخش/صف + پیامک) |
| `* * * * *` | **v37:** تور ایمنی پوشِ اعلان‌هایی که لحظهٔ ساخت، سرویس پوش خاموش بوده + ضربان نشانگر کرون (تنظیمات → عمومی) |
| `*/5 * * * *` | یادآوری سفارش‌های بی‌پذیرش به مدیران (اعلان/پوش/پیامک — یک‌بار به‌ازای سفارش) |
| `30 3 * * *` | پاکسازی دوره‌ای (OTP/اعلان/لاگ‌ها + روتیشن لاگ) |

> **مهم:** لازم نیست برای هر رخداد کرون جدا بزنید — فقط همان یک خطِ `schedule:run` هر دقیقه کافی است؛ خودِ لاراول تشخیص می‌دهد کدام فرمان در هر لحظه باید اجرا شود.
> **صف کار (queue) لازم نیست:** هیچ job ای در صف قرار نمی‌گیرد (اعلان/پیامک/پوش همزمان اجرا می‌شوند)؛ پس `queue:work`/supervisor صف نمی‌خواهد.

```bash
# گزینهٔ ا (کرون سیستم — روی سرور مشترک):
(crontab -l 2>/dev/null; echo "* * * * * cd /path/to/caffeinet && php artisan schedule:run >> /dev/null 2>&1") | crontab -

# گزینهٔ ب (پایدارتر — supervisor):
[program:caffeinet-schedule]
command=php /path/to/caffeinet/artisan schedule:work
autostart=true
autorestart=true
numprocs=1
redirect_stderr=true
stdout_logfile=/path/to/caffeinet/storage/logs/scheduler.log
stopwaitsecs=3600
```

---

## ۵) چک‌لیست امنیت پروداکشن

همهٔ این موارد در **پنل → وضعیت سیستم (`/admin/system`)** به‌صورت زنده قابل بازبینی‌اند:

- [ ] `APP_ENV=production`
- [ ] `APP_DEBUG=false` (در غیر این صورت stack trace فاش می‌شود)
- [ ] `APP_KEY` تولید شده (فراموش نشود؛ توکن‌ها/نشست‌ها به آن وابسته‌اند)
- [ ] `FILE_ENCRYPTION_KEY` مستقل تولید و در جای امن (vault) نگهداری شود
- [ ] HTTPS فعال و هدر `X-Forwarded-Proto` از پروکسی عبور کند (اپ به همهٔ پروکسی‌ها trust دارد)
- [ ] `php artisan files:encrypt` اجرا شده (بک‌فیل فایل‌های قدیمی → پوشش ۱۰۰٪)
- [ ] `chmod 600 .env`
- [ ] پورت MySQL فقط localhost؛ دسترسی ریموت بسته شود
- [ ] فایروال: فقط ۸۰/۴۴۳ باز؛ ۹۰۰۰ (php-fpm) و دیتابیس نباید expose باشند
- [ ] بکاپ شبانه: `database` + `storage/app/private` + `.env` (رمزنگاری‌شده خارج از سرور)
- [ ] مانیتورینگ سلامت: `GET /up` باید ۲۰۰ بدهد (هر ۱ دقیقه)
- [ ] لاگ‌ها: `LOG_CHANNEL=daily` + `LOG_DAYS=14` (روتیشن حجمی هم داخل `system:cleanup` هست)

### هدرهای امنیتی (فعال به‌صورت پیش‌فرض — فاز ۱۱)

`X-Content-Type-Options`، `X-Frame-Options`، `Referrer-Policy`، `Permissions-Policy` و **CSP** با هش اسکریپت ضد-FOUC تم.
اگر اسکریپت درون‌خطی جدید اضافه کردید، هش `sha256-…` آن را به `app/Http/Middleware/SecurityHeaders.php` بیفزایید.

### محدودیت نرخ (فعال)

| لایه | سقف |
|---|---|
| OTP | ۲/دقیقه هر شماره + ۱۰/ساعت هر IP |
| API احرازشده | ۱۲۰/دقیقه هر کاربر |
| ورود پنل‌ها | ۵ تلاش ناموفق → ۶۰ ثانیه تعلیق |
| تیکت/پیام تیکت | ۱۰ و ۲۰ در دقیقه |

---

## ۶) Nginx (نمونه)

```nginx
server {
    listen 443 ssl http2;
    server_name example.ir;

    root /var/www/caffeinet/public;
    index index.php;

    ssl_certificate     /etc/letsencrypt/live/example.ir/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/example.ir/privkey.pem;

    client_max_body_size 20m;   # پیوست تیکت 15MB + سربار

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/run/php/php8.3-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known) { deny all; }
    location ~* ^/(storage/logs|app/private) { deny all; }  # مسیر داخلی
}

server {
    listen 80;
    server_name example.ir;
    return 301 https://$host$request_uri;
}
```

---

## ۷) درگاه پرداخت و پیامک (پس از نصب)

هیچ کلیدی در `.env` لازم نیست — همه از **پنل مدیریت → تنظیمات**:

1. **پیامک:** پرووایدر (log/kavenegar/fraasms) + کلید API و خط فرستنده — تست زنده از صفحهٔ قالب‌های پیامک.
2. **پرداخت:** درایور shetabit (zarinpal/…) + مرچنت‌کد.
3. **کمیسیون:** قواعد سراسری/اختصاصی از «قواعد کمیسیون».

---

## ۸) بکاپ و بازیابی

```bash
# بکاپ شبانه (cron 04:30)
0 4 * * * /path/to/backup.sh
#!/usr/bin/env bash
mysqldump --single-transaction -u caffeinet -p"$DB_PASS" caffeinet | gzip > /backups/db-$(date +\%F).sql.gz
tar -czf /backups/files-$(date +\%F).tar.gz -C /path/to/caffeinet storage/app/private
tar -czf /backups/env-$(date +\%F).tar.gz -C /path/to/caffeinet .env
# چرخش ۳۰ روزه
find /backups -mtime +30 -delete
```

بازیابی: سورس + `composer install` → بازیابی `.env` + دیتابیس + `storage/app/private` → `migrate --force` → `config:cache`.
فایل‌های رمزنگاری‌شده با همان `FILE_ENCRYPTION_KEY` بازیابی می‌شوند — کلید را جدا از بکاپ نگه دارید.

---

## ۹) ارتقا (آپدیت نسخه)

```bash
cd /path/to/caffeinet
php artisan down            # صفحهٔ نگهداری
git pull origin main
composer install --no-dev --optimize-autoloader
php artisan migrate --force
php artisan db:seed --force   # idempotent — کاتالوگ + تصاویر WebP همگام می‌شوند
php artisan config:cache && php artisan route:cache
php artisan files:encrypt   # اگر فایل خام جدیدی هست
php artisan up
```

> **آپدیت از نسخه‌های قبل از v22:** اگر تصاویر قبلاً 403/404 می‌دادند، علت
> پوشهٔ واقعی `public/storage` (به‌جای symlink) در zip های قدیمی بود؛
> seed جدید آن را خودکار تعمیر می‌کند و URL ها به `/media/...` مهاجرت می‌کنند
> (فایل‌های آپلودی قبلی در همان `storage/app/public` می‌مانند و بدون تغییر کار می‌کنند).

> **v23 — تغییرات اپ مشتری:** (۱) حالت شب/روز با دکمهٔ خورشید/ماه در هدر اپ
> (کلید ذخیرهٔ `caffeinet-theme` مشترک با پنل‌ها؛ انتخاب اول از تم سیستم‌عامل)؛
> (۲) لغو سفارش از سمت مشتری حالا **دلیل الزامی** دارد (API: `reason` min 5)؛
> (۳) صفحهٔ پروفایل بازطراحی شد (هرو + آمار سفارش‌ها از `GET /me` → `orders_stats`).
> Client های خارجی که قبلاً `POST /orders/{id}/cancel` را بدون reason صدا می‌زدند
> از این پس پاسخ 422 می‌گیرند — reason را ارسال کنند.

> **v24 — تغییرات اپ مشتری:** (۱) ویجت موجودی کیف پول از هدر حذف شد
> (با مبالغ بزرگ چیدمان هدر به‌هم می‌ریخت؛ دسترسی از ناوبری پایین و کارت کیف پول صفحهٔ پروفایل)
> و در نتیجه فراخوانی `GET /wallet` در هر صفحه هم حذف شد؛
> (۲) در شیت شارژ، مبلغ انتخابی/دستی با جداکنندهٔ هزارگان زیر ورودی پیش‌نمایش می‌شود؛
> (۳) صفحهٔ پروفایل به «نمای کاربر» (هرو + آمار + منوی حساب) و صفحهٔ جدید `/app/profile/edit`
> (فرم ویرایش) تفکیک شد؛
> (۴) ثبت‌نام اولیه مستقیم به `/app/profile/edit?new=1` هدایت می‌شود و
> **تمام صفحات/عملیات اپ نیازمند پروفایل تکمیل‌اند**: فرانت (ریدایرکت خودکار) + بک‌اند
> (ثبت سفارش و شارژ از قبل؛ اکنون **ثبت/پاسخ تیکت** هم — پیام 422 «ابتدا اطلاعات
> پروفایل خود را کامل کنید.»). Client های خارجی تیکت‌زن بدون پروفایل کامل از این پس 422 می‌گیرند.

> **v25 — سامانهٔ اطلاع‌رسانی سه‌لایه:**
> (۱) **صدا**: اعلان جدید در پنل‌ها (مدیر/کافی‌نت/اپراتور/سازمان — نه اپ مشتری) با صدا
> اعلام می‌شود؛ صدای پیش‌فرض `public/assets/sounds/notify.mp3` یا فایل سفارشی از
> پنل تنظیمات → «اعلان‌ها» (تیک «صدای پیش‌فرض» ↔ آپلودر mp3/wav/ogg/m4a تا ۲MB).
> (۲) **نوتیف دستگاه (FCM)**: وقتی کاربر (مشتری یا پرسنل) آنلاین نیست، اعلان با
> Firebase Cloud Messaging به گوشی/ویندوز می‌رسد (PWA باید نصب/اجازه‌دار باشد).
> راه‌اندازی: تنظیمات → اعلان‌ها → پرووایدر Firebase + ۴ فیلد عمومی + Service Account JSON؛
> کاربران با دکمهٔ «فعال‌سازی نوتیف دستگاه» (زیر زنگ اعلان) توکن ثبت می‌کنند
> (API جدید: `POST/DELETE /api/v1/push/token`). توکن‌ها در جدول `push_tokens` (مهاجرت v25).
> (۳) **پیامک‌های رویدادی** (تنظیمات → پیامک → رویدادها): انتقال به کافی‌نت/اپراتور آفلاین،
> واریز حقوق/تسویه، یادآوری «درخواست بی‌پذیرش» (هر ۵ دقیقه با دستور
> `orders:notify-unaccepted` زمان‌بندی‌شده — ستون `orders.unaccepted_notified_at`) و
> قطع/وصل پیامک پاسخ تیکت (قالب‌های جدید در مرکز پیامک: `notify.*`).
> حضور کاربران با `users.last_seen_at` (میدل‌ور UpdateLastSeen) ردیابی می‌شود.
> همچنین عنوان/متن همهٔ اعلان‌ها به قالب‌های «رویدادی» با کلید اختصاصی تبدیل شد
> (هر بخش عنوان و متن خودش را دارد؛ برای پوش و اعلان مشترک است).
> لندینگ: بخش خدمات فقط **دسته‌بندی‌ها** را نمایش می‌دهد (کارت خدمت‌ها برای کاهش اسکرول موبایل حذف شد).
>
> **دپلوی v25 روی نصب موجود:** `php artisan migrate --force` (۳ مهاجرت:
> last_seen_at، unaccepted_notified_at، push_tokens) + `php artisan db:seed --force`
> (کلیدهای تنظیمات اعلان + قالب‌های notify.* پیامک). PHP باید به‌روی اینترنت
> برای FCM/OAuth گوگل دسترسی داشته باشد (درون ایران معمولاً نیاز به پروکسی سرور دارد).

> **v26 — نوتیف دستگاه سه‌سرویسی + انتخاب مدیر:**
> تنظیمات → «اعلان‌ها و پوش» → «سرویس نوتیف دستگاه» حالا **چهار حالت** دارد:
> (۱) **پیش‌فرض (وب‌پوش داخلی)** — پیشنهادی: کلیدهای VAPID با اولین ذخیره خودکار
> ساخته می‌شوند؛ سرور شما پیام رمزنگاری‌شده (RFC 8291/8292 — پیاده‌سازی خالص PHP،
> بدون هیچ وابستگی) مستقیم به سرویس پوش مرورگر (FCM/Mozilla/Apple) می‌فرستد؛
> بدون ثبت‌نام در سرویس بیرونی و بدون محدودیت تعداد. (۲) **Pusher Beams**
> (instance id + primary key از پنل پوشر؛ انتشار با Bearer به interest `user-{id}`).
> (۳) **Firebase (FCM)** — مثل v25. (۴) **خاموش**.
> جریان کلاینت (هر ۵ لایه): دکمهٔ «فعال‌سازی نوتیف دستگاه» زیر زنگ/شیت اعلان →
> push-client.js بر اساس سرویس فعال اشتراک می‌سازد (VAPID / Beams SDK / FCM SDK —
> همگی vendor محلی) → ثبت در `push_tokens` (ستون‌های جدید provider/p256dh/auth).
> «حالت پیش‌فرض» روی ویندوز/اندروید/کروم/ادج/فایرفاکس + iOS 16.4+ PWA نصب‌شده کار می‌کند.
>
> **دپلوی v26 روی نصب موجود:** `php artisan migrate --force` (۱ مهاجرت:
> extend_push_tokens_for_multi_provider) — seed لازم نیست. اگر از فایربیس به
> «پیش‌فرض» مهاجرت می‌کنید کاربران باید یک‌بار دوباره دکمهٔ فعال‌سازی بزنند
> (اشتراک با کلید VAPID تازه ساخته می‌شود؛ فرایند خودکار است چون اجازه قبلاً داده شده).

> **v28 — حذف نرم/دائم + رفع ۵ باگ:**
> (۱) **حذف نرم‌افزاری** در ۷ بخش پنل مدیریت کل (مشتریان/کافی‌net‌ها/کارکنان/
> مدیران/سازمان‌ها/تیکت‌ها/سفارش‌ها): دکمهٔ «حذف» در هر ردیف → پیام تأیید با
> **هشدار وابستگی‌ها** → انتقال به «حذف‌شده‌ها» (دکمهٔ سربرگ هر صفحه) با
> «بازگردانی» و «حذف دائم» (فایل‌ها و گفتگوهای وابسته پاک می‌شوند).
> آبشارها: حذف مدیر کافی‌net ⇒ حذف کافی‌net (و بالعکس)؛ حذف سازمان ⇒
> کافی‌net‌های زیرمجموعه مستقل می‌شوند؛ حذف مشتری ⇒ سفارش‌ها/تیکت‌هایش؛
> حذف سفارش ⇒ تصاویر و گفتگو. کاربر حذف‌شده فوراً از همهٔ ورودها خارج می‌شود.
> (۲) **کافی‌net‌های معرفی‌شده توسط سازمان** حالا اطلاعات ورود (کاربر مدیر)
> در ویرایش ساخته می‌شود — ریشهٔ نرسیدن اعلان‌ها به این کافی‌net‌ها.
> (۳) واگذاری سفارش به اپراتور حالا به **مشتری** هم اعلان می‌دهد.
> (۴) گفتگوی تیکت در پنل‌ها: پیام‌های خودِ کاربر سمت راست.
> (۵) منوی «فازهای بعدی» پنل سازمان/کافی‌net حذف شد؛ چک‌باکس خام ساعت کاری مخفی.
>
> **دپلوی v28 روی نصب موجود:** `php artisan migrate --force` (۲ مهاجرت:
> add_soft_deletes_to_core_entities + add_soft_deletes_to_staff_assignments)
> — seed لازم نیست؛ داده‌ای تغییری نمی‌کند (فقط ستون deleted_at اضافه می‌شود).

> **v29 — آستانهٔ آفلاین انتخابی + تایم‌زون + حذف دوره‌ای لاگ‌ها:**
> (۱) **آستانهٔ «آفلاین»** (تنظیمات ← اعلان‌ها و پوش) حالا انتخابی است: سوییچ روشن →
> ورودی **ثانیه** (پیش‌فرض ۱۸۰ = همان ۳ دقیقهٔ قبلی)؛ سوییچ خاموش → **لحظه‌ای**
> (بلافاصله بعد از آخرین درخواست، کاربر آفلاین = پوش/پیامک آفلاین حتی با پنل باز).
> مقدار قدیمی `offline_minutes` خودکار به ثانیه مهاجرت می‌کند.
> (۲) **منطقهٔ زمانی سامانه** (تنظیمات ← عمومی): انتخاب از فهرست (پیش‌فرض UTC =
> رفتار قبلی؛ برای ایران Asia/Tehran). همهٔ تاریخ‌ها، شمسی‌سازی و زمان‌بندی‌های
> خودکار (۰۳:۳۰ پاکسازی و…) با همین منطقه ارزیابی می‌شوند.
> (۳) **حذف دوره‌ای لاگ‌ها**: کارت «حذف دوره‌ای» روی صفحات «لاگ پیامک‌ها» و
> «لاگ فعالیت» — نگهداشت (روز، مثل ۲۰) ذخیره می‌کنید، قدیمی‌تر از آن هر شب
> ۰۳:۳۰ خودکار حذف می‌شود (از قدیمی‌ترین) و دکمهٔ «پاکسازی قدیمی‌ها الان»
> همان‌جا اجرا می‌کند (`system:cleanup --scope=…`، فقط همان لاگ).
> (۴) **پیوست تیکت در اپ مشتری**: فایل انتخاب‌شده حالا چیپ مشخص زیر جعبهٔ متن
> نشان داده می‌شود (نام + حجم فارسی + دکمهٔ حذف) — قبلاً روی موبایل پیدا نبود.
> (۵) دارک‌مود تیکت: بکگراند اضافی پشت پیام مشتری حذف شد.
>
> **دپلوی v29 روی نصب موجود:** `php artisan migrate --force` (۲ مهاجرت:
> add_offline_threshold_settings + add_timezone_setting) — seed لازم نیست.
> نکته: `schedule:work` را بعد از دپلوی یک‌بار ری‌استارت کنید تا منطقهٔ زمانی
> جدید و کلیدهای پاکسازی را ببیند.

> **v30 — سازگاری MySQL + اصلاحات مودال حذف:**
> (۱) **رفع خطای MySQL** `FUNCTION ... julianday does not exist` در صفحهٔ
> پروفایل کارکنان (`/admin/operators/{id}`) و پروفایل مشتری: عبارت میانگین
> زمان تحویل حالا بر اساس درایور ساخته می‌شود (MySQL → `TIMESTAMPDIFF`،
> SQLite → `julianday`) — هلپر `sql_avg_minutes()`.
> (۲) **روند کاری (چارت)** روی MySQL هم درست گروه‌بندی می‌شود: `strftime`
> فقط مال SQLite است؛ برای MySQL از `DATE_FORMAT` و فرمول هفتگی
> معادل `FLOOR((DAYOFYEAR+6-WEEKDAY)/7)` استفاده می‌شود (کلیدهای باکت
> در دو درایور یکسان‌اند).
> (۳) **مودال حذف**: دکمهٔ «انصراف» واقعاً می‌بندد (`modal.remove()`)؛ قفل
> اسکرول صفحه و لیسنر Escape بعد از بستن (انصراف/بستن/Escape/پس‌زمینه)
> خودکار پاک می‌شود؛ چیدمان هشدارهای کهربایی و پدینگ سربرگ مودال
> «حذف‌شده‌ها» اصلاح شد (trash.css v2).
> (۴) **باگ پنهان کارکنان**: کلیک روی دکمهٔ «حذف» در فهرست کارکنان، به‌جای
> مودال تأیید صفحه را به پروفایل می‌برد (ناوبری ردیف، دکمه‌ها را مستثنا
> نمی‌کرد) — رفع شد (operators/index.js v29).
>
> **دپلوی v30 روی نصب موجود:** فقط جایگزینی فایل‌ها — **مهاجرت جدیدی
> نیست** (`php artisan migrate --force` بی‌ضرر است). برای MySQL نیازی به
> تغییر .env نیست؛ کوئری‌ها خودشان بر اساس درایور ساخته می‌شوند.
> مرورگرها کش trash.css/trash.js را با `?v=2` رد می‌کنند.

---

## ۱۰) عیب‌یابی سریع

| علامت | بررسی |
|---|---|
| سفارش‌ها در پخش می‌مانند | `schedule:work` فعال است؟ (پنل → وضعیت سیستم) |
| فایل دانلود نمی‌شود/«رمزگشایی ناموفق» | `FILE_ENCRYPTION_KEY` تغییر کرده؟ (گزارش در laravel.log) |
| ۴۲۹ زیاد | سقف API در `app/Providers/AppServiceProvider.php` |
| خطای CSP در کنسول | هش اسکریپت جدید را به SecurityHeaders اضافه کنید |
| تصویر ۴۰۳/۴۰۴ می‌دهد | از روت `/media/...` استفاده می‌شود؟ URL های قدیمی `/storage` به symlink وابسته‌اند؛ `db:seed` دوباره اجرا کنید (تعمیر خودکار symlink) |
| لاگ حجیم | `system:cleanup` + `LOG_DAYS` |

---

## v31 — صفحهٔ گفتگوی تمام‌صفحه (تلگرام‌گونه) + اصلاحات CSS

> **تغییر مهم UX:** صفحهٔ سفارش مشتری (`/app/orders/{id}`) به یک صفحهٔ
> گفتگوی تمام‌صفحه شبیه چت تلگرام تبدیل شد:

- **هدر اپ و ناوبری پایین در این صفحه حذف شد** (`no-chrome`) — فقط گفتگو.
- **سربرگ گفتگو فیکس بالا**: آواتار + نام اپراتور/کافی‌net + وضعیت +
  دکمهٔ «اطلاعات سفارش» (ⓘ) + **دکمهٔ بازگشت (back) در سمت چپ** → فهرست سفارش‌ها.
- **نوار ارسال فیکس پایین** (با احترام به ناحیهٔ امن iOS)؛ ناحیهٔ پیام‌ها تنها
  بخش اسکرول‌شونده است — هیچ اسکرول صفحه‌ای نیست.
- **جابه‌جایی بخش‌ها:** «روند سفارش»، «خلاصه سفارش» (هزینه‌ها + داده‌های فرم +
  دلیل لغو)، «مدارک ارسالی» و «تاریخچه پرداخت» به شیت **«اطلاعات سفارش»**
  (دکمهٔ ⓘ سربرگ) منتقل شدند. لغو درخواست: در وضعیت‌های پیش از اتصال (پخش/صف)
  زیر کارت وضعیت داخل صفحه، و در گفتگوی فعال پایین شیت اطلاعات.
- **وضعیت‌های پیش از اتصال** (پخش ۶۰ ثانیه‌ای / صف تعیین‌تکلیف / پرداخت legacy):
  کارت‌ها وسط ناحیهٔ پیام‌ها نمایش داده می‌شوند (بدون نوار ارسال).
- **نظرسنجی پس از تحویل** انتهای جریان گفتگو (بالای بنر «فقط-خواندن»).
- سربرگ پیش از اتصال نام خدمت + شماره سفارش را نشان می‌دهد (payload چت:
  `order.service` و `order_number` به ChatService اضافه شد).
- **CSS ارسالی مالک:** `.assign-timer .t-num` (padding 20px 0) و
  `.t-num small` (11.5px / margin-top -12px) در app.css v19؛
  `.trash-list-head` بدون `display: flex` در trash.css v3.
- **رفع باگ پیش‌نمایش Sandbox:** میان‌افزار `FixPreviewAssetQuery` الگوی
  دو «؟»ی URLهای استاتیک (`?XTransformPort=8000?v=NN`) را اصلاح می‌کند
  (قبلاً CSS/JS پشت گیت‌وی ۵۰۲ می‌شدند). در پروداکشن کاملاً بی‌اثر است.

> **دپلوی v31 روی نصب موجود:** فقط جایگزینی فایل‌ها — **مهاجرت و تغییر
> دیتابیسی نیست**. کش مرورگر با نسخه‌های جدید رد می‌شود
> (chat.css?v=17 / app.css?v=19 / order-detail.js?v=17 / order-chat.js?v=16 /
> trash.css?v=3).

## v32 — رفع دو باگ چت + CSS ارسالی مالک

> **۱) ترتیب پیام‌ها بعد از رفرش:** ریشه در بک‌اند بود — رابطهٔ
> `Conversation::messages()` به‌طور پیش‌فرض `oldest('id')` دارد؛ در
> `ChatService::messages()` دستور `latest('id')` با آن ترکیب می‌شد
> (`ORDER BY id ASC, id DESC` → برندهٔ اولی یعنی **ASC**) و سپس `reverse()`
> ترتیب را معکوس می‌کرد → بارگذاری اولیهٔ گفتگو **نزولی** (جدیدترین بالا)
> برمی‌گشت؛ پولینگ افزایشی (after_id) درست بود به همین دلیل مشکل فقط
> بعد از رفرش دیده می‌شد. **رفع:** `reorder('id', 'desc')` مرتب‌سازی
> پیش‌فرض رابطه را خنثی می‌کند → آخرین ۶۰ پیام واقعاً نزولی گرفته می‌شود و
> `reverse()` آن را صعودی می‌کند. هر سه مسیر (اپ مشتری / پنل اپراتور /
> پنل مدیر و کافی‌net) از همین سرویس می‌گذرند و همگی اصلاح شدند.

> **۲) رفتن نوار ارسال زیر صفحه در حالت نصب PWA (برخی گوشی‌ها):** در
> برخی WebViewهای standalone مقدار `100dvh` بزرگ‌تر از پنجرهٔ واقعی گزارش
> می‌شود → بدنه اسکرول می‌گرفت و input زیر cut-off می‌رفت. **رفع:**
> اسکریپت برازش ارتفاع در `order-detail.js` — ارتفاع پوستهٔ چت با
> `window.innerHeight` و `visualViewport.height` (سازگار با کیبورد مجازی:
> نوار ارسال بالای کیبورد می‌ماند، مثل تلگرام) تنظیم می‌شود؛ `100dvh`
> فقط fallback بدون-JS است. چسبیدن به پایین (وقتی تصویری دیر لود می‌شود
> یا کیبورد باز/بسته می‌شود اسکرول روی جدیدترین پیام می‌ماند) با
> ResizeObserver + رویداد visualViewport در `order-chat.js`.

> **۳) CSS ارسالی مالک:**
> - `chat.css` — `.bubble` با `white-space: normal` (خط جدیدِ متن پیام
>   با `pre-wrap` روی خود `.msg-text`/`.msg-caption` حفظ می‌شود).
> - `landing.css` — `.ticker-track` با سرعت `200s`.

> **دپلوی v32 روی نصب موجود:** فقط جایگزینی فایل‌ها — **مهاجرت و تغییر
> دیتابیسی نیست**. کش مرورگر با نسخه‌های جدید رد می‌شود
> (chat.css?v=18 / order-chat.js?v=17 / order-detail.js?v=18 /
> landing.css?v=6).


## v33 — نظرسنجی کامل + مرکز نظرسنجی‌ها + پخش هوشمند + چت سفارش‌های لغوشده

> **۱) نظرسنجی مشتری کامل شد (اپ مشتری):** کارت نظرسنجی در انتهای گفتگو حالا
> <b>دو گروه ستاره</b> دارد — «امتیاز کلی تجربه» (ستاره‌های اصلی) و «امتیاز
> اپراتور» (اگر سفارش اپراتور داشته باشد، اختیاری) — و زیر آن
> <b>گزینه‌های دلایل به‌صورت چک‌باکس‌های کارتی</b> نمایش داده می‌شود: برای
> امتیازهای ۴/۵ نقاط قوت («برخورد مناسب»، «انجام سریع کار»، …) و برای ۱/۲
> نقاط ضعف («تأخیر در انجام کار»، …)؛ برای ۳ هر دو. گزینه‌ها از جدول جدید
> `rating_options` می‌آیند و **مدیریت کامل آنها (افزودن/غیرفعال/حذف) از پنل
> مدیریت کل** انجام می‌شود. امتیازهای ثبت‌شده دلایل را به‌صورت اسنپ‌شات
> (`[{id,title,type}]`) ذخیره می‌کنند → حذف/ویرایش گزینه تاریخچه را خراب
> نمی‌کند.

> **۲) مرکز نظرسنجی‌ها (منوی جدید «نظرسنجی‌ها»):** مدیر کل همهٔ نظرات را با
> امتیاز کافی‌net + امتیاز اپراتور + دلایل تیک‌خورده + متن دیدگاه می‌بیند:
> چیپ‌های آماری (کل/میانگین/میانگین اپراتور/نظرات منفی)، توزیع امتیاز
> (کلیک‌پذیر = فیلتر)، پرتکرارترین دلایل و جدول با فیلترهای کامل: امتیاز
> (۱..۵/عالی/ضعیف)، امتیاز اپراتور، کافی‌net، اپراتور، دلیل، متن دیدگاه،
> بازهٔ تاریخ، مرتب‌سازی. مدیر کافی‌net هم نسخهٔ خودش را در
> `/coffeenet/{id}/ratings` می‌بیند. در صفحهٔ «سفارش‌ها» هم ستون امتیاز +
> فیلتر امتیاز (مثلاً «بدون امتیاز») + کارت کامل نظرسنجی در مودال جزئیات
> اضافه شد.

> **۳) اعلان امتیاز پایین:** امتیاز در حد آستانه یا پایین‌تر → اعلان فوری
> به مدیر کل و مدیر کافی‌net مربوطه (+ پوش آفلاین). آستانه/روشن‌خاموش از
> تنظیمات.

> **۴) پخش هوشمند بر اساس امتیاز (تنظیمات ← تب جدید «نظرسنجی و امتیاز»):**
> سفارش‌های جدید را می‌توان به کافی‌netهای با امتیاز خوب محدود کرد — سه
> حالت: <b>هوشمند ترکیبی</b> (پیش‌فرض/پیشنهادی: فقط واجد شرایط‌ها؛ اگر هیچ‌کدام
> نبود به همه پخش می‌شود)، <b>سخت‌گیرانه</b> (فقط واجد شرایط‌ها؛ وگرنه صف
> تعیین‌تکلیف) و <b>اولویت‌بندی</b> (همه می‌گیرند، امتیاز بالاتر زودتر
> اطلاع می‌یابد). حداقل میانگین امتیاز (۱..۵)، حداقل تعداد رأی و سیاست
> کافی‌netهای بدون امتیاز معتبر قابل تنظیم است. آمار هر کافی‌net ۵ دقیقه
> کش می‌شود و با ثبت هر نظر فوراً تازه می‌شود. تصمیم هر پخش در تاریخچهٔ
> سفارش ثبت می‌شود.

> **۵) چت سفارش‌های لغوشده:** مدیر کل و مدیر کافی‌net گفتگوی سفارش‌های لغوشده
> را می‌بینند (فقط-خواندنی، با نوار رز «این سفارش لغو شده است» + دلیل لغو).
> تب «لغوشده» به فهرست گفتگوها اضافه شد و دکمهٔ <b>«گفتگوی سفارش»</b> به مودال
> جزئیات سفارش (مدیر کل) و ردیف‌های سفارش (مدیر کافی‌net) برای دسترسی سریع.
> اپ مشتری و پنل اپراتور مثل قبل است (چت لغوشده نمی‌بینند).

> **دپلوی v33 روی نصب موجود:** جایگزینی فایل‌ها ← اجرای
> `php artisan migrate --force` (**۲ مهاجرت جدید**: جدول `rating_options` با
> گزینه‌های پیش‌فرض + ستون‌های `operator_rating`/`options` روی
> `order_ratings` + ۸ کلید تنظیمات گروه `ratings`). seed لازم نیست
> (گزینه‌ها و تنظیمات داخل مهاجرت idempotent ساخته می‌شوند؛ راهنمای پنل
> تازه با `php artisan db:seed --class=GuideSeeder` — اختیاری). کش مرورگر
> با نسخه‌های جدید رد می‌شود (chat.css?v=19 / app.css?v=20 (front) /
> order-detail.js?v=19 / orders index.js?v=29 / show.js?v=3 /
> ratings.css?v=1 / ratings/index.js?v=1 / settings css/js v18/v20 /
> show.js چت v=16).

---

## v34 — رفع باگ نظرسنجی‌ها + اعلان‌های گفتگو و وضعیت

> **۱) رفع باگ صفحهٔ نظرسنجی‌ها (admin/ratings و coffeenet/{id}/ratings):**
> کوئری قبلی `whereNotNull(coffeenet_id)` باعث می‌شد نظرسنجی سفارش‌هایی که
> کافی‌net ندارند (پردازش مستقیم توسط مدیر کل — مثلاً تغییر وضعیت دستی به
> «تحویل‌شده») در مرکز نظرسنجی‌ها **نمایش داده نشود** در حالی که در مودال
> جزئیات سفارش دیده می‌شدند. این شرط حذف شد؛ چنین ردیف‌هایی با برچسب
> «بدون کافی‌net» نمایش داده می‌شوند و در آمار/توزیع هم لحاظ می‌شوند.

> **۲) جستجوی صفحهٔ نظرسنجی‌ها تمام‌عرض شد:** باگ چیدمان (flex-1 + min-w-0
> بین ۵ سلکت و فیلتر تاریخ) باعث می‌شد اینپوت سرچ در دسکتاپ به ~۴۰px له
> شود. حالا سرچ در ردیف مستقل تمام‌عرض بالای فیلترهاست (اندازهٔ راحت،
> فوکوس کهربایی، دارک‌مود کامل). ردیف فیلترها هم در موبایل نرم wrap می‌شود.

> **۳) اعلان «پیام جدید» در گفتگوی سفارش (این‌برنامه‌ای + نوتیف دستگاه):**
> قبلاً پیام چت فقط رویداد Realtime داشت؛ اگر گیرنده آنلاین نبود هیچ
> اعلانی نمی‌گرفت. حالا با هر پیام، طرف مقابل (مشتری ↔ اپراتور/مدیر کافی‌net/
> مدیر کل) اعلان زنگ می‌گیرد؛ اگر **آفلاین** باشد نوتیف دستگاه (FCM /
> وب‌پوش / Pusher Beams — طبق سرویس فعال در تنظیمات ← اعلان‌ها) روی
> گوشی/ویندوز او می‌رود و کلیک روی آن گفتگوی همان سفارش را باز می‌کند.
> برای هر کاربر و هر گفتگو حداکثر **یک اعلان خوانده‌نشده** نگه داشته
> می‌شود (بدون اسپم زنگ) و با باز کردن خود گفتگو خوانده می‌شود.

> **۴) اعلان تغییر وضعیت سفارش برای همهٔ وضعیت‌ها:** قبلاً فقط تحویل/لغو/
> تکمیل اعلان داشت. حالا «شروع کار» و «نیازمند اطلاعات» هم اعلان اختصاصی
> به مشتری می‌فرستند (+ پوش آفلاین) — هم از مسیر مدیر کل/مدیر کافی‌net و هم
> اپراتور. لغو سفارش توسط مشتری هم حالا به مدیران کافی‌net/مدیر کل اعلان
> می‌دهد.

> **دپلوی v34 روی نصب موجود:** جایگزینی فایل‌ها — **بدون مهاجرت** (فقط
> کد). کش مرورگر با نسخه‌های جدید رد می‌شود (ratings.css?v=2 /
> ratings/index.js?v=2 / sw.js v1.1.7). برای رسیدن نوتیف به گوشی، سرویس
> نوتیف دستگاه (تنظیمات ← اعلان‌ها ← فایربیس/پیش‌فرض/Beams) باید فعال و
> کاربران یک‌بار در اپ/پنل «فعال‌سازی نوتیف» را بزنند (توکن دستگاه ثبت
> می‌شود).

## v35 — قانون «برنامه باز → فقط اعلان داخل برنامه» (نوتیف سیستمی فقط وقتی برنامه بسته است)

> **گزارش مالک:** «وقتی برنامه باز است باز هم نوتیف سیستمی می‌آید؛ نیازی
> به آن نیست چون نوتیف داخل برنامه کافی است. فقط وقتی برنامه بسته و
> آفلاین است می‌خواهم نوتیف سیستمی برود — برای همهٔ بخش‌ها.»
> این نسخه دقیقاً همین قانون را پیاده می‌کند — **دو لایهٔ مستقل:**

> **۱) ریشهٔ باگ (سمت سرور) — حالت «آفلاین لحظه‌ای» شکسته بود:**
> تنظیم «آستانهٔ آفلاین» وقتی خاموش می‌شد، آستانه ۰ ثانیه می‌شد؛ اما
> میدل‌ور حضور حداکثر هر ۶۰ ثانیه `last_seen_at` را می‌نویسد → کاربر حتی
> وسط استفادهٔ فعالِ برنامه هم «آفلاین» تلقی می‌شد → پوش دستگاه همیشه
> ارسال می‌شد → نوتیف سیستمی روی گوشیِ برنامهٔ-باز ظاهر می‌شد.
> **رفع:** حالت خاموش = «کوتاه (۹۰ ثانیه)» و مقدار دستی هم کف ۹۰ ثانیه
> گرفت (کمتر از آن با دورهٔ نوشتن ۶۰ ثانیه‌ای بی‌معنا بود). صفحهٔ باز هر
> ۲۵-۳۰ ثانیه درخواست می‌زند → هیچ‌وقت آفلاین تلقی نمی‌شود → **سرور پوش
> نمی‌فرستد.** (مهاجرت ۲۰۲۶_۰۹_۱۶ برچسب‌ها/مقادیر زیرکف را اصلاح می‌کند؛
> متن تنظیمات هم جدید شد.)

> **۲) تضمین نهایی (سمت مرورگر) — Service Worker هوشمند (sw.js v1.1.8):**
> حتی اگر سرور به هر دلیلی پوش بفرستد، SW قبل از نمایش نوتیف سیستمی
> پنجره‌های **باز و مرئی** را با `MessageChannel` صدا می‌زند؛ اگر صفحهٔ
> همان کاربر (تطبیق `uid` داخل پیام پوش — برای همهٔ سرویس‌ها: وب‌پوش/
> Beams/FCM) تا ۴ ثانیه ACK بدهد، **نوتیف سیستمی اصلاً نمایش داده نمی‌شود**
> و پیام به خود صفحه تحویل می‌شود: رویداد `cn:push` + توست درون‌برنامه‌ای +
> تازه‌سازی آنی زنگ اعلان و چتِ باز. اگر صفحه‌ای جواب ندهد یا برنامه
> بسته/پس‌زمینه باشد → نوتیف سیستمی مثل قبل روی گوشی/ویندوز (با کلیک →
> گفتگوی همان سفارش). دکمهٔ «پیش‌نمایش نوتیف» در تنظیمات مثل قبل همیشه
> نمایش می‌دهد (تست صریح مدیر).

> **۳) تجربهٔ درون‌برنامه‌ای آنی:** زنگ اعلان اپ مشتری و ۴ پنل با رویداد
> `cn:push` همان لحظه بج را تازه می‌کنند؛ صفحهٔ چت (اپ مشتری + پنل‌ها) اگر
> پیام مال همین گفتگو باشد (`oid`) بلافاصله پیام‌ها را می‌گیرد. پوش هر
> سرویس حالا `uid` (گیرنده) و چت‌ها `oid` (سفارش) را هم داخل دادهٔ پیام
> دارند (وب‌پوش داخلی + FCM + Beams web.data).

> **دپلوی v35 روی نصب موجود:** جایگزینی فایل‌ها → `php artisan migrate
> --force` (فقط مهاجرت برچسب آستانه — بدون داده). SW با باز کردن هر صفحه
> خودش به‌روز می‌شود (v1.1.8 + پاکسازی کش‌های قدیمی). کش‌باست:
> push-client.js?v=6 / notifications.js v4 (اپ) و v16 (پنل‌ها) /
> order-chat.js?v=18 / chat/show.js?v=17 / settings/index.js?v=21 /
> sw.js v1.1.8. **نکتهٔ مهم:** پوش فقط یک‌بار برای کاربرانی ارسال می‌شود
> که قبلاً «فعال‌سازی نوتیف دستگاه» را زده‌اند (توکن ثبت‌شده) — کاربرانِ
> جدید باید یک‌بار فعال کنند.

---

## v36 — تضمین رسیدن پوش سیستمی به آفلاین‌ها + حضور آنلاین + نشانگر کرون

**گزارش مالک:** در v35 وقتی برای مشتریِ آفلاین (برنامه بسته) پیام می‌رفت، نوتیف سیستمی نمی‌آمد؛ فقط با باز کردن برنامه اعلان داخل اپ دیده می‌شد. در v34 نوتیف همیشه می‌آمد (حتی برای آنلاین‌ها).

**ریشه:** مقدار پیش‌فرض آستانهٔ آفلاین ۱۸۰ ثانیه بود (کف v35 هم ۹۰) — یعنی مشتری تا ۳ دقیقه بعد از بستن برنامه «آنلاین» تلقی می‌شد و چون تصمیم پوش فقط «در لحظهٔ ساخت اعلان» گرفته می‌شد، پوش هیچ‌وقت فرستاده نمی‌شد.

**رفع — سه لایه (همه با همان آستانهٔ تنظیمات؛ یک منبع حقیقت):**

> **۱) آستانهٔ سریع:** کف ۴۵ ثانیه + پیش‌فرض ۴۵ (مهاجرت ۲۰۲۶_۰۹_۱۷ مقدارهای ۹۰/۱۸۰ را به ۴۵ می‌رساند و کش تنظیمات را تازه می‌کند). فاصلهٔ نوشتن `last_seen` هم از ۶۰ به ۲۰ ثانیه کم شد (UpdateLastSeen) — برنامهٔ بسته حداکثر تا ۴۵ ثانیه بعد قطعاً «آفلاین» تشخیص داده می‌شود.

> **۲) پوش فوری:** لحظهٔ ساخت اعلان، اگر گیرنده آفلاین بود پوش همان‌جا می‌رود (مثل v34 برای آفلاینِ واقعی) و ردیف `pushed=1` علامت می‌خورد.

> **۳) تضمین نهایی — `notifications:flush-pending` (هر دقیقه در پس‌زمینه):** اعلان‌های «در انتظار پوش» (گیرنده در لحظهٔ ارسال آنلاین بود) دوباره چک می‌شوند؛ به‌محض آفلاین‌شدن گیرنده (طبق همان آستانه) پوش با **متن آخرین پیام** می‌رود (داخل هر اجرا ۳ گذار با فاصلهٔ ۱۵ ثانیه). خوانده‌شده‌ها چک نمی‌شوند؛ فقط ۱۵ دقیقهٔ اخیر. **وابسته به کرون هاست است** (بند ۴) — بدون کرون، فقط پوشِ فوری کار می‌کند.

**حضور آنلاین (درخواست مالک):**
- جزئیات مشتری / اپراتور / مدیر کافی‌نت (کارکنان کافی‌نت) / مدیران — چیپ «آنلاین» (نقطهٔ سبز چشمک‌زن) یا «آفلاین · X دقیقه پیش» با title تاریخ دقیق (`panel-ui.css?v=15`)
- داشبورد مدیریت کل: دو کارت **«مشتریان آنلاین»** و **«کارکنان آنلاین»** (نقش + موبایل + لینک پروفایل) — همان آستانه
- اپ مشتری: سربرگ چت اگر اپراتور متصل آنلاین باشد نقطهٔ سبز + رنگ سبز (`operator.online` در API — `chat.css?v=20` + `order-chat.js?v=19`)

**نشانگر سلامت کرون (تنظیمات → عمومی):** هر اجرای `schedule:run` ضربان `system.cron.last` را تازه می‌کند — سبز (پالس) = فعال + آخرین اجرا؛ **قرمز = کرون جاب هاست تنظیم نیست** + خط فرمان کپی‌کردنی. (همین مهاجرت نقش `admin` را هم می‌سازد تا صفحهٔ مدیران روی نصب‌های بدون مدیر دستیار خطا ندهد.)

**دپلوی v36 روی نصب موجود:** جایگزینی فایل‌ها → `php artisan migrate --force` (مهاجرت ۲۰۲۶_۰۹_۱۷: آستانهٔ ۴۵ + برچسب‌ها + نقش admin + کلید ضربان؛ کش تنظیمات هم خودکار تازه می‌شود). سپس **حتماً کرون هر-دقیقه‌ای `schedule:run` را روی هاست بگذارید** و از تنظیمات → عمومی سبز شدن نشانگر را چک کنید — بدون آن، پوشِ تاخیریِ لایهٔ ۳ و موتور پخش سفارش کار نمی‌کنند.

---

## v37 — بازگشت پوش سیستمی به رفتار v34 (همیشه ارسال) + رفع نشانگر کرون + آفلاین‌شدن آنی

**گزارش مالک (۳ مورد):**
1. **(P0)** در v36 برای مشتریِ آفلاین (برنامه بسته) نوتیف سیستمی باز هم نمی‌آمد؛ فقط با باز کردن برنامه اعلان درون اپ می‌آید. دستور صریح مالک: اگر درست نشد، بخش نوتیف سیستمی به **v34** برگردد — «چندتا اضافه بیاد به‌مراتب بهتر از اینکه هیچ‌وقت نیاد».
2. **(P0)** کرون روی هاست درست ست شده (`cd … && php artisan schedule:run`) و روی سایت‌های دیگر همان کار می‌کند، ولی نشانگر تنظیمات همچنان قرمز است.
3. آفلاین‌شدن دیر تشخیص داده می‌شود (~۲ دقیقه) — سریع‌تر شود.

**رفع ۱ — پوش سیستمی همیشه (دقیقاً رفتار v34، همهٔ لایه‌ها):**
- `sw.js` → **v1.2.0**: هر پیام push که به Service Worker برسد «همیشه» نوتیف سیستمی نمایش می‌یابد — منطق ACK/تشخیص پنجرهٔ باز (v35) کاملاً حذف شد (همان منطق روی هاست واقعی باعث از دست رفتن نوتیف می‌شد). زنگ درون‌برنامه/چت از پول خودش تازه می‌شود.
- `push-client.js` → v7: هندلر PUSH_DELIVER/ACK حذف شد (فقط ثبت/تجدید اشتراک + **بیکن حضور جدید**).
- سرور (`NotificationService` + `PushManager`): لحظهٔ ساخت هر اعلان، پوش دستگاه **برای همهٔ گیرندگان** ارسال می‌شود — بدون هیچ چک آنلاین/آفلاین (آنلاین بودن فقط «نمایش حضور» است).
- `notifications:flush-pending` به «تور ایمنی» ساده شد: فقط ردیف‌هایی که لحظهٔ ساخت، سرویس پوش خاموش بوده را بعد از فعال شدن سرویس می‌فرستد.
- نتیجه: نوتیف سیستمی روی گوشی/ویندوز برای همهٔ اعلان‌ها (چت/وضعیت سفارش/تیکت/…) **می‌آید — برنامه باز یا بسته**. اگر برنامه باز باشد کاربر هم زنگ درون‌برنامه و هم نوتیف سیستم‌عامل را می‌بیند (همان v34).

**رفع ۲ — نشانگر کرون (دو مسیر مستقل + اجرای دستی + ردپای فایل):**
- مسیر ۱: رویداد `ScheduledTaskStarting` (AppServiceProvider) — در شروع «هر» تسکِ سرِرسید فایر می‌شود؛ یعنی تا وقتی `schedule:run` روی هاست اجرا شود، ضربان قطعاً ثبت می‌شود (حتی اگر تسک heartbeat به هر دلیلی اجرا نشود).
- مسیر ۲: تسک `cron-heartbeat` در `routes/console.php` (هر دقیقه).
- هر دو روی `system.cron.last` (DB) + **`storage/app/cron-trace.log`** (ردپای فایل برای عیب‌یابی — آخرین ۵ ردپا در حالت قرمز نمایش داده می‌شود) می‌نویسند.
- **دکمهٔ «اجرای دستی (تست)»** در کارت کرون (تنظیمات → عمومی): همان `schedule:run` را یک‌بار از داخل پنل اجرا می‌کند + خروجی کنسول را نشان می‌دهد. راهنمای تشخیص: اگر با دکمه سبز شد ولی بعد از ~۳ دقیقه دوباره قرمز شد → برنامه سالم است و **کرونِ هاست به سایت نمی‌رسد** (خروجی `php artisan schedule:run` را بدون `>> /dev/null 2>&1` در ترمینال ببینید تا خطا پیدا شود).

**رفع ۳ — آفلاین‌شدن آنی:**
- **بیکن `POST /presence-offline`** (web، CSRF-معاف): `push-client.js` روی `pagehide` / `visibilitychange→hidden` با `sendBeacon` اعلام می‌کند «برنامه بسته شد» → `last_seen_at` همان لحظه به گذشته برمی‌گردد → داشبورد/جزئیات/چت فوری «آفلاین». اپ مشتری توکن Sanctum را در بدنه می‌فرستد؛ پنل‌ها کوکی نشست.
- `UpdateLastSeen` نوشتن `last_seen` را از ۲۰ به **۱۰ ثانیه** افزایش داد — پشتیبانِ بیکن برای مرورگرهایی که pagehide نمی‌دهند (iOS-PWA).
- آستانه ۴۵ ثانیه دست‌نخورده ماند (مهاجرت v36) — حالا فقط معنای «نمایش حضور» دارد، نه ارسال پوش.

**دپلوی v37 روی نصب موجود:** فقط **جایگزینی فایل‌ها** — بدون مهاجرت جدید. کاربرانِ فعلی:
- `sw.js` با اولین بازدید بعدی به v1.2.0 به‌روز می‌شود (نسخهٔ کش‌ها عوض شد؛ بعدش `clients.claim` همهٔ صفحات را می‌گیرد).
- توکن‌های دستگاهِ موجود معتبرند — پوش از همان لحظه برای همه ارسال می‌شود.
- برای نشانگر کرون: بعد از جایگزینی، یک‌بار تنظیمات → عمومی → «اجرای دستی (تست)» را بزنید؛ اگر سبز شد و بعد ~۳ دقیقه قرمز شد، مشکل از رسیدن کرونِ هاست است (بند ۴ + راهنمای داخل کارت).
